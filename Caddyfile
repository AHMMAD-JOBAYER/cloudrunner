localhost {
    # API proxy (do not strip prefix)
    reverse_proxy /api/* backend:3000

    # Health check
    reverse_proxy /health backend:3000

    # SPA frontend
    reverse_proxy /* frontend:8000
}
